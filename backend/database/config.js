const config = {
    db: {
        host: "localhost",
        port: 3306,
        user: "root",
        password: "dev_password",
        database: "divblox-onboarding",
    },
};
export default config;