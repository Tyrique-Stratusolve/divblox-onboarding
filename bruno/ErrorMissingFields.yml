info:
  name: ErrorMissingFields
  type: http
  seq: 7

http:
  method: POST
  url: http://localhost:3000/people
  body:
    type: json
    data: |-
      {
        firstName: ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
